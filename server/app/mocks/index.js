const keymap = require('./keymap.mock');
const emails = require('./emails.mock');
const modals = require('./modals.mock');
const machines = require('./machines.mock');

module.exports = {
  keymap,
  emails,
  modals,
  machines,
};
